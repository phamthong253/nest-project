import { ResponseMapperInterceptor } from './response-mapper.interceptor';

describe('ResponseMapperInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseMapperInterceptor()).toBeDefined();
  });
});
